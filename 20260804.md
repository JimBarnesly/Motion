# Motion build progress — 2026-08-04

Times are Pacific/Auckland. Entries report verified work completed during each interval.

## 07:45–08:00

- Connected the local repository to `https://github.com/JimBarnesly/Motion`, preserved and merged the remote MIT licence history, and pushed all existing Motion commits to `main` at `2b6bb52`.
- Installed Rust/Cargo 1.97.1 for the current user and refreshed the npm workspace with `npm ci` (0 reported vulnerabilities).
- Ran the Tauri spike through native dependency resolution. Compilation now reaches `glib-sys` and correctly reports the missing host GLib development package; GTK/WebKit system packages still require valid sudo access on `192.168.68.60`.
- Started three non-overlapping implementation streams: production Tauri shell, Web UI/app-service integration, and restart/offline E2E coverage.
- Added this 15-minute progress log and scheduled verified interval updates through the 15:30 handoff.

Current next step: integrate and review the three implementation streams, then run the complete test/typecheck/build suite and push each coherent change.

## 08:00–08:09

- Added closed-shape validation and normalisation for Web schema-v1 workspace restores, including fixtures and tests for hostile markup, duplicate IDs, invalid hierarchy cycles, and hostile IDs entering HTML attributes (`407c510`).
- Updated desktop workspace scripts so typechecking and builds first compile the application-service dependency, and pinned the Tauri spike dependency graph in `Cargo.lock` (`14c000d`, `c2224df`).
- Updated development/status/risk documentation to reflect the typed Tauri boundary, canonical SQLite service integration, native CI matrix, and remaining desktop packaging gaps (`0a59dcd`).
- Verified the repository suite locally: `npm test` passed 50 tests, `npm run typecheck` passed, and `npm run build` passed.

Active blocker: local native Tauri compilation and packaging still require the missing GTK/WebKit development packages and administrator access on `192.168.68.60`; the desktop bridge also still depends on external Node 24 and starts a service process per IPC request.

Current next step: replace the process-per-request desktop bridge with a bundled persistent runtime or measured Rust-owned service, then verify a packaged Linux create/edit/link/search/attach/restart/export/restore flow with networking blocked.

## 08:09–08:24

- Changed the desktop bridge to retain one application-service process across IPC requests, with tests covering multiple durable requests and error recovery (`ce024e0`).
- Made attachment promotion recoverable after interruption by reconciling committed metadata with staged content on the next operation, with storage and application-service regression coverage (`f87b252`).
- Connected the Web UI to canonical native search, export, attachment, and verified-backup operations while retaining honest browser fallbacks (`dc59569`, `27c7b42`).
- Verified the current workspace with `npm test`: all 55 tests passed, including the new desktop runner, attachment recovery, native Web adapter, and backup/restore cases.

Active blocker: native Tauri compilation and Linux packaging remain unverified locally because the required GTK/WebKit development packages are unavailable on `192.168.68.60`. Bundled-runtime and browser E2E changes are present but still uncommitted and are not reported as completed.

Current next step: finish and validate the bundled Node runtime and Playwright E2E changes, then run native package builds on supported x86-64 and ARM64 Linux runners and exercise the packaged offline workflow.

## 08:24–08:37

- Added Playwright coverage for the offline Web workflow and wired it into CI (`8c25496`).
- Added preparation and verification for a bundled Node runtime used by the desktop shell, including a runtime-preparation test and CI integration (`b91bd2f`).
- Preserved pages through trash and restore across the Web workspace, desktop service runner, and core migration path, with regression tests (`721762a`).
- Added native desktop icon assets and tightened the offline-asset check to enforce the pinned build URL policy (`6f75bb0`).

Active blocker: no new passing full-suite or native package-build result was recorded in this interval. Local native Tauri packaging remains unverified because the GTK/WebKit development packages are unavailable on `192.168.68.60`; further Web and E2E changes are currently uncommitted.

Current next step: finish the active Web/E2E changes, run the complete test, typecheck, build, and Playwright suite, then verify native packages on supported x86-64 and ARM64 Linux runners.

## 08:37–08:52

- Added Web unit and Playwright regression coverage proving that trash state survives reload and that a trashed page can be restored (`236bb6f`).
- GitHub Actions run `30851258449` passed the complete verification job: typecheck, unit/integration tests, production build, Chromium end-to-end test, local-only source check, secret scan, and benchmark smoke fixture.
- The same CI run prepared the checksum-verified bundled Node runtime, built the Web assets, passed the desktop TypeScript/IPC checks and native Rust tests, compiled release binaries for x86-64 and ARM64, and produced both architecture-specific `.deb` packages.

Active blocker: AppImage packaging failed on both architectures because Tauri could not find a square icon; consequently the overall CI run failed and package artifacts were not uploaded. The repository is otherwise clean and `main` matches `origin/main` at `f4c57c0`.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 08:52–09:07

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 09:07–09:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 09:22–09:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 09:37–09:52

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 09:52–10:07

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 10:07–10:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 10:22–10:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 10:37–10:52

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 10:52–11:08

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 11:08–11:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 11:22–11:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 11:37–11:52

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 11:52–12:07

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 12:07–12:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 12:22–12:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 12:37–12:52

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 12:52–13:07

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 13:07–13:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 13:22–13:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 13:37–13:53

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 13:53–14:08

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 14:08–14:22

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 14:22–14:37

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 14:37–14:52

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 14:52–15:07

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.

## 15:07–15:23

- No new product commits, local test/build results, or GitHub Actions runs were produced in this interval; `main` remains aligned with `origin/main` at `f4c57c0`.

Active blocker: native CI run `30851258449` remains failed because AppImage packaging could not find a square icon on either architecture, so package artifacts were not uploaded.

Current next step: correct the Tauri square-icon configuration, rerun the native CI matrix, then smoke-test the uploaded x86-64 and ARM64 packages offline.
