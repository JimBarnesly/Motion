{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The main Motion window has no plugin filesystem, shell, dialog, opener, or network capabilities; it uses only registered application commands.",
  "windows": ["main"],
  "permissions": []
}
