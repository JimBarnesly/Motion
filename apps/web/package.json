{
  "name": "@motion/web",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --strict --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext test/app-adapter-types.ts",
    "test": "node --check app.js && npm run typecheck && node --test test/*.test.mjs"
  }
}
