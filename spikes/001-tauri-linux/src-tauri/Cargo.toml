[package]
name = "motion-tauri-linux-spike"
version = "0.0.1"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lib]
name = "motion_tauri_linux_spike_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
